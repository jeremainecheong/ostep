# Java 21 course

Interactive course in three tracks (JLS · JVM/HotSpot · virtual threads and Java 21 features), 17 lessons, built from primary sources. Open `index.html`. Notes and quiz scores are stored in the browser (`java21:` keys); export from `notes.html`. Build a self-contained copy: `python3 build.py <outdir>`.
